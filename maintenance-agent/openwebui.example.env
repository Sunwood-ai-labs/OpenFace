# This placeholder keeps Docker Compose portable in environments where the
# optional maintenance-agent service is not configured yet. Never commit keys.
OPEN_WEBUI_BASE_URL=http://host.docker.internal:3000
OPEN_WEBUI_DEFAULT_MODEL=glm-4.7

